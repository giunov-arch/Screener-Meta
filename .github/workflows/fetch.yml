name: Fetch Dati Borsa Italiana

on:
  workflow_dispatch:
  schedule:
    - cron: '0 17 * * 1-5'

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install deps
        run: |
          pip install -r requirements.txt 2>/dev/null || true
          pip install -r github-screener-italia/requirements.txt 2>/dev/null || true
          pip install yfinance==0.2.54 pandas==2.2.3 numpy==1.26.4 requests curl-cffi

      - name: Run collector (quello che funzionava)
        run: |
          set -x
          # collector.py è quello stabile, fetcher.py è solo copia
          if [ -f collector.py ]; then
            echo "Uso collector.py in root"
            python collector.py
          elif [ -f github-screener-italia/collector.py ]; then
            echo "Uso github-screener-italia/collector.py"
            python github-screener-italia/collector.py
          elif [ -f fetcher.py ]; then
            echo "Fallback a fetcher.py"
            python fetcher.py
          else
            echo "Nessun collector trovato" && ls -lh && exit 1
          fi
          ls -lh dati/ data/ 2>/dev/null || true

      - name: Commit e push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f dati/snapshot.json data/snapshot.json data/italian_stocks.json data/italian_stocks.csv data/last_update.json 2>/dev/null || true
          git add -f dati/ data/ 2>/dev/null || true
          git status
          if git diff --staged --quiet; then
            echo "Nessun cambiamento"
            exit 0
          fi
          git commit -m "Aggiornamento dati: $(date -u +'%Y-%m-%d %H:%M UTC') - collector"
          git push
